import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const CapabilityLevelsManager: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Capability Levels Manager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Capability Levels management interface will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default CapabilityLevelsManager;