import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const AdvancePaymentTypesManager: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Advance Payment Types Manager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Advance Payment Types management interface will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default AdvancePaymentTypesManager;