import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const WorkflowTemplatesManager: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Workflow Templates Manager</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Workflow Templates management interface will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default WorkflowTemplatesManager;